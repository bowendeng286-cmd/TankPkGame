# AGENTS.md

This file provides guidance to agents when working with code in this repository.

## 项目概述
纯前端 HTML5 坦克对战游戏（Tank Trouble 克隆），无构建系统，直接在浏览器中运行。

## 运行方式
直接用浏览器打开 `index.html`，无需构建或安装依赖。

## 完成通知规则
- 任务真正完成后，在发送最终回复前，应触发完成通知
- 完成通知需要播放 3 声提示音，并弹出提示框告知用户任务已经完成
- 如果任务被阻塞、只完成部分内容、或仅进行分析说明，则不要触发完成通知

## AI 协作开发工作规范（强制）

以下规范强制纳入本仓库的 `AGENTS.md` 执行规则中，AI 在本仓库工作时必须遵守；如与其他低优先级说明冲突，以本节要求为准。

### 目标
- AI 的任务是协助用户完成编程项目开发。
- 鉴于用户编程基础有限，所有改动应尽量清楚、稳妥、可验证。
- AI 应将本节视为仓库内的高优先级执行守则。

### 协作原则
1. 每次开始修改前，先说明准备修改的内容、会影响哪些文件、以及可能存在的风险。
2. 尽量做小改动，不要一次性重写过多代码。
3. 如果存在多种实现方式，优先选择最简单、最稳妥、最容易维护的方案。
4. 不要默认用户能直接看懂复杂代码；解释关键点时要尽量使用用户容易理解的方式。

### 代码完成后的要求
1. 每次写完代码后，AI 必须先自行审查一遍，再交给用户验证。
2. 自查重点包括：
   - 逻辑是否正确
   - 有没有明显语法错误
   - 有没有遗漏边界情况
   - 有没有影响到其他文件或功能
   - 有没有不必要的复杂写法
3. 如果发现改动中存在疑点，AI 必须先主动说明，不能直接把问题留给用户。
4. 完成自查后，再让用户实际运行程序或测试，作为最终审查。
5. 一轮任务真正结束后，AI 必须更新本文件中的本轮任务状态，明确写成“任务已完成，用户已确认”。
5. 每次任务真正完成后，AI 必须在 `AGENTS.md` 中补充记录本轮开发经验，不能省略。
6. 记录内容必须包括本轮开发中踩到的坑，既包括编码时遇到的坑，也包括与用户沟通和理解需求时遇到的坑。
7. 与用户沟通相关的坑包括但不限于：用户描述与 AI 理解不一致、同一组件或功能的称呼不同导致理解偏差、需求边界表达不完整、用户默认前提未明说等。
8. 这些记录应尽量具体、详细、可复用，目标是在后续开发时减少重复犯错并提升开发效率。
9. 除了坑点记录，AI 还必须补充本轮开发的内容摘要和进度状态，让后续继续开发时能快速理解当前阶段。
10. 如果本轮没有明显坑点，也要明确写明“本轮未发现明显坑点”，并记录本轮完成内容与当前进度。

### 用户负责的部分
1. 用户主要负责按照说明运行程序、点击界面、执行实际测试。
2. 如果用户反馈报错或行为异常，AI 需要根据反馈继续修正。
3. 由于用户编程基础有限，AI 应尽量明确告知：该看哪里、该运行什么、预期结果是什么。

### Git 工作流
1. 不要直接在 `main` 分支上随意改代码。
2. 每个任务尽量单独创建一个分支，例如 `feature/login-form`、`fix/api-timeout`、`refactor/config-loader`。
3. 修改前先检查当前状态，确保清楚有哪些文件会被影响。
4. 修改后先查看 `git diff`，确认改动符合预期。
5. 每次只提交一个清晰的小改动，不要把很多无关内容混在一起。
6. 提交信息要说明改动目的，例如 `fix login validation`、`add config loader`、`refactor error handling`。
7. 在用户确认可以之后，再合并到主分支。
8. 本仓库的 git 和分支管理默认尽量由 AI 全权负责，包括建分支、提交、合并、整理改动和处理常规仓库操作。
9. 用户不擅长管理仓库，也不会主动查看代码和仓库改动；AI 应将仓库管理工作视为自己的职责范围。
10. 用户的主要职责是在 AI 完成一次改动并说明后，反馈“没问题”或“还需要改”。
11. 在用户明确确认之后，后续的合并、整理和其他 git 操作由 AI 负责，不需要用户自行处理。
12. AI 应主动负责仓库节奏控制，不要把仓库维护、分支整理、合并判断等事务推给用户。
13. 如果某个 git 操作确实需要用户参与，AI 必须用最简单、最明确的话告诉用户只需要做什么，不要让用户自己判断仓库细节。
14. 每次完成一轮任务后，AI 必须明确告诉用户现在需要进行测试，并说明用户应看哪里、怎么测、预期结果是什么。
15. 只有在用户测试后明确表示“可以”“没问题”或同等确认后，AI 才能执行合并操作；这里的合并既包括合并到 `main`，也包括合并到其他支线。
16. 即使当前已经在某个支线上工作，只要开始新的功能开发或 Debug 任务，AI 也必须再新建一个新分支，并在该新分支上工作，不能直接在原支线上继续叠加新任务。
17. 如果当前工作分支是从另一条支线切出的子分支，那么用户对当前子分支说“可以”后，AI 只能先把该子分支合并到它的直接上级支线，不能因为这次确认就直接一路合并到 `main`。
18. 子分支合并回直接上级支线后，AI 必须主动总结当前上级支线中的全部修改内容（既包括上级支线原本已有的修改，也包括刚从子分支合并回来的修改），再单独询问用户该上级支线是否要继续合并到 `main`。
19. 用户对某一层分支的确认，只代表同意该层级的合并，不自动代表同意更高层级继续合并到 `main`；是否并入 `main` 必须在对应层级再次明确确认。

### 推荐的实际流程
1. 先确认任务目标。
2. 为本轮任务创建专用分支；如果当前已经在某个支线上，但要开始新的功能开发或 Debug，也要继续新建分支，而不是直接沿用原分支。
3. 开始修改代码。
4. AI 自行审查修改。
5. 在 `AGENTS.md` 中记录本轮开发内容、进度和踩坑经验。
6. 明确告诉用户如何运行和验证，并直接说明现在需要用户进行测试。
7. 用户实际运行并反馈结果。
8. AI 根据反馈继续修正。
9. 用户明确确认“可以”之后，AI 先判断当前分支是直接面向 `main` 的支线，还是某条支线下面的子分支。
10. 如果当前是子分支，则先只合并回直接上级支线，随后总结上级支线当前全部修改，再单独询问用户该上级支线是否继续合并到 `main`。
11. 如果当前就是直接面向 `main` 的支线，或用户在上一步已经明确确认上级支线可以继续合并，则再由 AI 负责提交并合并到对应目标分支。

### 需要避免的事情
1. 不要一次性改太多文件。
2. 不要在没有解释的情况下做大范围重构。
3. 不要跳过自查直接让用户测试。
4. 不要假设用户能自动发现问题，应尽量提前指出风险和验证方法。
5. 不要在任务完成后忘记把本轮踩坑经验、开发内容和进度写入 `AGENTS.md`。
6. 不要只写笼统结论，坑点记录必须尽量细，避免后续无法复用。

### 简短原则
先想清楚，再改代码。先自查，再让用户运行。先用分支管理改动，再通过 `git diff` 和小提交确认质量。

再次强调：这个仓库以 AI 主导管理为默认协作方式，用户主要负责确认结果，不负责仓库运维。

## 开发记录维护规则（强制）

AI 在每次任务真正完成后，必须继续维护 `AGENTS.md` 中的开发记录，作为后续开发的重要上下文来源。

### 必须记录的内容
1. 本轮任务做了什么，改了哪些文件，当前做到哪一步。
2. 本轮开发进度，例如：已完成、部分完成、待用户验证、等待新需求、等待进一步修正。
3. 本轮踩到的编码坑，例如：变量作用域误判、脚本加载顺序问题、全局状态依赖遗漏、样式或交互联动理解错误、调试中发现的隐藏约束。
4. 本轮踩到的沟通坑，例如：用户说法与代码中的实际命名不同、用户描述偏口语导致功能边界理解偏差、AI 自己对用户意图做了错误推断、同一个概念在不同上下文下含义不同。
5. 后续开发时需要特别注意的事项，避免重复踩坑。

### 记录要求
1. 记录要尽量详细，不能只写过于笼统的一句话。
2. 记录必须可供未来直接复用，让后续 AI 能快速理解历史问题和当前状态。
3. 每条开发记录的标题时间必须精确到分钟，格式应类似 `2026-04-18 15:10`，不能只写日期不写时间。
4. 每条开发记录还必须写明本轮对应的工作分支名称，避免后续无法判断改动来自哪条支线。
5. 如果某个坑是因为 AI 自己理解失误造成的，也必须明确写出误解点和修正后的正确理解。
6. 如果本轮用户表达和 AI 理解存在偏差，必须把双方各自的表达方式一并记下，帮助后续对齐语义。
7. 如果本轮没有新增坑点，也要记录本轮未出现明显新坑，并补充本轮开发内容和进度。

### 目的
- 通过持续记录开发内容、进度和坑点，为后续开发提供稳定上下文。
- 降低 AI 重复犯错的概率。
- 降低因为命名差异、口语描述差异、需求理解偏差带来的沟通成本。
- 提升后续任务的开发效率和协作准确性。

### 推荐记录模板
后续每次任务真正完成后，AI 应优先按以下模板追加记录，避免遗漏关键上下文：

```markdown
## 开发记录

### [日期 时间（精确到分钟）] 任务标题
- 工作分支：`branch-name`
- 本轮开发内容：
  - 做了什么
  - 修改了哪些文件
  - 关键改动点是什么
- 当前进度：已完成 / 待用户验证 / 部分完成 / 等待进一步修正 / 等待新需求
- 编码坑点：
  - 没有则写：本轮未发现明显编码坑点
  - 有的话尽量写清楚触发条件、错误理解、修正方式、后续如何避免
- 沟通坑点：
  - 没有则写：本轮未发现明显沟通坑点
  - 有的话写清楚用户原话或原始表达、AI 最初如何理解、后来如何确认出正确理解
- 后续注意事项：
  - 下次继续开发时要优先注意什么
  - 哪些命名、边界、前提条件容易再次误解
```

AI 追加记录时应尽量保持格式稳定，便于后续快速查阅。

## 开发记录

### [2026-04-18] 合并 AI 工作规范并补充开发记录要求
- 本轮开发内容：
  - 将 `AI工作规范.md` 的内容强制纳入 `AGENTS.md`。
  - 补充了“任务完成后必须记录开发内容、进度、编码坑点、沟通坑点”的强制规则。
  - 新增了开发记录维护规则，并补充了统一的推荐记录模板，方便后续持续追加。
  - 本轮修改文件为 `AGENTS.md`。
- 当前进度：已完成
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 过程中遇到一次工具环境兼容问题：部分检索工具出现 `wasm-simd is not enabled` 报错，导致无法依赖该工具完成核对；本轮改用 `git diff` 和直接读取文件内容完成自查。后续如果再次遇到相同情况，应优先切换到 `read` 或 `git diff` 这类更稳定的验证方式。
- 沟通坑点：
  - 用户最初要求“将 AI工作规范.md 文件中的规范强制纳入 AGENTS.md 规范文件中”，当时更偏向于“把独立规范全文并入主规范”。
  - 后续用户补充“再加上让AI每次完成任务后在 AGENTS.md 文件中写入内容，为自己在本轮开发中踩到的坑……以及要写开发的内容和进度”，说明用户真实目标不只是合并旧规范，还希望把“持续记录开发经验”也写成强制执行规则。
  - 这类需求属于分两轮逐步说清的情况。后续遇到类似“先合并规范，再补充记录要求”的描述时，应主动意识到用户可能还关心“执行后的落地方式”，例如是否需要模板、是否需要记录示例。
  - 本轮还发现一个表达习惯差异：用户说“写入内容”，真实含义更接近“建立长期可追加的记录机制”，而不仅仅是临时加一句要求。后续应优先从“是否需要长期维护结构”角度理解此类表述。
- 后续注意事项：
  - 后续每次任务真正完成后，除了完成代码或文档本身，还必须继续更新本文件中的“开发记录”。
  - 记录时不要只写结论，必须尽量写清楚误解是怎么产生的、后来怎么修正的。
  - 如果用户使用口语化表达或对同一功能有自己的叫法，应在记录中保留用户原始表达，减少后续语义偏差。

### [2026-04-18] 删除 AI工作规范.md 原始文件
- 本轮开发内容：
  - 删除了仓库中的 `AI工作规范.md`。
  - 保留 `AGENTS.md` 作为唯一规范载体，避免后续同时维护两份近似规则文件。
  - 本轮修改文件为 `AI工作规范.md` 和 `AGENTS.md`。
- 当前进度：已完成
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这次是文档删除操作，但仍需同步更新 `AGENTS.md` 中的开发记录，否则会与前面新增的强制规则不一致。后续凡是完成真实任务，即使只是删文件或改文档，也要记得补记录。
- 沟通坑点：
  - 本轮未发现明显沟通坑点。
  - 用户表达“删除AI工作规范.md”非常明确，边界也清晰，不需要再推断是否保留副本或迁移内容；因为相关规范内容已经提前并入 `AGENTS.md`，所以直接删除原文件即可。
- 后续注意事项：
  - 后续如果再清理类似“已被主文件吸收”的独立说明文档，应先确认内容是否已经完整迁移，再执行删除。
  - 删除说明类文件后，若仓库流程依赖该文件名，应顺手检查是否还有引用；本轮未发现额外引用要求。

### [2026-04-18] 优化中等和困难 AI 的威胁预测开销
- 本轮开发内容：
  - 在 `js/ai.js` 中优化了 AI 威胁评估流程，重点处理“中等/困难难度更卡”时每帧重复做高成本子弹轨迹预测的问题。
  - 给子弹未来轨迹预测增加了共享缓存：同一帧内多台 AI 面对同一颗子弹时，如果子弹位置、速度、生命周期、反弹次数和预测时长都没变化，就直接复用已有轨迹结果，不再重复调用 `traceBulletSegments()`。
  - 增加了一个安全的距离预筛：如果按当前子弹速度和预测时长计算，理论上都不可能在本次预测窗口内接近当前坦克，就直接跳过后续完整轨迹分析，减少中高难 AI 在子弹较多时的无效计算。
  - 本轮修改文件为 `js/ai.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次的性能问题不是单个 AI 内部某段代码“特别慢”这么简单，更大的开销来自“多台 AI 对同一颗子弹重复做同样的未来轨迹推演”。如果只盯着某个函数本身，很容易漏掉这种跨 AI 重复计算的成本来源。后续排查性能问题时，要优先看是否存在“对象级重复运算”。
  - 这类优化不能靠粗暴降低 AI 思考频率，否则虽然会更省算力，但很容易让困难难度的闪避和反应明显变笨。本轮因此优先选了“缓存已有结果”和“只跳过理论上不可能命中的远距离子弹”两种更稳妥的做法。
- 沟通坑点：
  - 用户原话是“我觉得坦克AI的中等和困难电脑有点卡，需要优化一下算法”。这里的“卡”有两种常见含义：一种是帧率卡、算法开销大；另一种是 AI 行为卡顿、决策发愣。
  - 本轮结合“需要优化一下算法”的说法，先按“性能开销偏大”理解并处理。如果用户后续反馈不是更流畅，而是“AI 还是会犹豫、走位发呆”，则应改按“行为决策节奏”方向继续查，不要把两种“卡”混为一谈。
- 后续注意事项：
  - 后续若继续优化 AI 性能，优先检查 `ai.js` 中的威胁预测、反弹射击搜索和闪避规划是否还能继续复用结果，而不是先削弱难度参数。
  - 用户验证时应重点观察“中等/困难 AI 对战时整体是否更流畅”，同时确认 AI 仍然会正常躲子弹、瞄准和开火，避免只优化了性能却悄悄削弱行为质量。

### [2026-04-18] 新增 AI 性能评估悬浮面板
- 本轮开发内容：
  - 在 `js/main.js` 中新增了一个网页悬浮性能面板，按数字键 `1` 即可显示或隐藏。
  - 面板会持续显示最近一段时间的平均 FPS、帧间隔、主线程单帧工作耗时、AI 总耗时、AI 耗时占比，以及当前帧的 AI 相关统计项。
  - 在 `js/ai.js` 中补充了轻量级统计计数，包含子弹威胁扫描次数、远距离预筛跳过次数、轨迹请求/缓存命中/实际计算次数、寻路次数、反弹射击搜索次数，方便直接观察优化前后差异。
  - 本轮修改文件为 `js/main.js`、`js/ai.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 只做性能优化但没有可观测指标时，很容易陷入“感觉上好像有变化，但说不清到底改善了多少”的状态。后续处理 AI 或渲染性能问题时，应尽量先补最低成本的观测面板或统计项，再继续做针对性优化。
  - 性能统计本身也会消耗一点点时间，所以统计逻辑要尽量轻，只做简单计数和少量时间采样，避免“为了测性能反而把性能测坏”。本轮因此没有做重型 profiler，而是采用每帧轻量聚合、显示层限频更新的方式。
- 沟通坑点：
  - 用户这次反馈“肉眼很难看出有什么变化”，说明用户真正缺的不是“再猜一次怎么优化”，而是“一个能直接验证优化是否生效的观察工具”。
  - 如果用户对某次优化反馈“看不出来”“感觉差不多”，后续不应立刻继续叠加新优化，而应优先判断是否缺少量化指标、可视化开关或复现实验方式。本轮正确理解应是“先建立评估工具，再继续比较效果”。
- 后续注意事项：
  - 后续如果继续调 AI 性能，优先在同一局势下对比悬浮面板中的 `ai work avg/max`、`trace req/hit/compute` 和 `pathfinds/ricochet`，不要只看主观手感。
  - 如果用户后续还希望更精细对比，可考虑再加“手动重置样本窗口”或“导出一段时间统计结果”的功能，但目前先保持工具简单、低侵入。

### [2026-04-18] 回退 AI 性能优化与监测面板
- 本轮开发内容：
  - 按用户要求回退了此前对中等/困难电脑的 AI 性能优化，不再保留共享子弹轨迹缓存和远距离预筛逻辑。
  - 同时移除了 `js/main.js` 中新增的 AI 性能悬浮面板，以及 `js/ai.js` 中与之配套的统计埋点，恢复到原来的更新流程。
  - 本轮修改文件为 `js/ai.js`、`js/main.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次回退说明“性能优化”不一定只影响帧率，还可能改变 AI 的行为质量。即使理论上只是减少计算，只要影响到威胁评估时机、轨迹复用方式或筛选范围，用户也可能直接感受到 AI 变弱、变迟钝或表现失真。
  - 回退此类改动时，不能只删一半逻辑。比如如果只删 `main.js` 的面板而保留 `ai.js` 的统计调用，或反过来只删统计函数不删调用，就会留下无意义依赖甚至隐藏问题。后续做“加监测工具”类改动时，要把“功能代码”和“观测代码”视为一组管理。
- 沟通坑点：
  - 用户这次明确表示“现在的优化已经严重影响了AI的性能”，这里的“性能”实际更接近“AI 表现/实力/质量”，不再是之前理解里的“运行性能/FPS/算力开销”。
  - 这说明同一个词“性能”在用户语境里可能既指运行流畅度，也可能指 AI 强弱和行为效果。后续如果用户再说“AI 性能变差了”，需要优先确认他说的是“电脑变卡”还是“AI 变笨了”，避免沿着错误方向继续优化。
- 后续注意事项：
  - 后续若再尝试做 AI 优化，应优先保证行为结果和原版一致，再考虑算力开销，避免再次出现“更省算力但 AI 手感变差”的情况。
  - 用户验证时应重点看简单、中等、困难电脑之间的对战表现是否恢复到之前更自然的状态，以及数字键 `1` 是否已经不再弹出性能面板。

### [2026-04-18] 恢复困难 AI 的“神闪”躲弹手感
- 本轮开发内容：
  - 在 `js/ai.js` 中让困难难度优先使用旧式方向评分闪避链路，不再完全依赖当前的规划式闪避器；只有旧式方向选不出有效结果时，才回退到现有规划式闪避。
  - 把旧式闪避相关状态变量重新接回实际流程，包括 `_lastDodgeDir`、`_dodgeCommitTime`、`_dodgeElapsed`、`_dodgeOrigin`、`_dodgeStartAngle` 和 `_shouldBreakDodgeLock()`，让困难 AI 能更早锁定一个逃生方向并连续执行。
  - 在 `js/constants.js` 中只增强了困难难度的闪避参数，提升预判时间、预览距离和连续闪避倾向，同时降低部分切换/倒车惩罚，以恢复更“神仙”的躲子弹手感。
  - 本轮修改文件为 `js/ai.js`、`js/constants.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 旧式闪避函数虽然还留在仓库里，但候选方向生成器已经不在主流程中，不能只把 `_scoreDodgeDirection()` 接回去就算完成。本轮因此补了一个最小化方向候选生成逻辑，用于把“子弹切线闪避”和“远离威胁点”重新组织起来。
  - 旧式闪避和规划式闪避共存时，如果状态变量不重置干净，很容易出现“上一帧还在旧式锁方向，下一帧切到规划器又保留旧状态”的混杂问题。本轮在旧式和兜底规划式之间显式重置了 `_lastDodgeAction`、`_dodgeCommitTime`、`_dodgeElapsed` 等状态，避免原地抖动或锁死。
- 沟通坑点：
  - 用户前面说“AI 性能变差”，实际并不是指运行帧率，而是主要指“困难 AI 躲子弹弱了许多”。如果继续沿着“性能/FPS/算力”去理解，会完全跑偏。
  - 用户后来进一步明确“主要是在躲子弹上弱了许多”，这帮助确认本轮应只聚焦困难 AI 的闪避恢复，而不是同时乱改射击、追击和其他进攻逻辑。后续遇到“AI 性能”这种说法时，应优先确认是在说运行性能还是行为表现。
- 后续注意事项：
  - 后续如果用户还觉得困难 AI 不够“神”，优先继续微调 `hard` 的旧式闪避候选方向、预判距离和切换阈值，不要先去动 `medium` 或攻击逻辑。
  - 用户验证时应重点观察困难 AI 是否会更早开始躲子弹、是否更少出现犹豫换向，同时确认不会出现明显卡墙、无限倒车或原地抖动。

### [2026-04-18] 删除误接回的远古闪避 AI 代码
- 本轮开发内容：
  - 按用户反馈，删除了上一轮误接回的旧式方向评分闪避逻辑，不再让困难 AI 使用那套远古版本的躲弹代码。
  - 同时清理了与之绑定的冗余辅助函数、状态字段、回合重置逻辑和常量，避免这些已经不打算再用的旧逻辑继续留在主流程周围。
  - 将 `hard` 难度的闪避参数恢复到删除前的规划式闪避版本，保持当前主 AI 逻辑一致。
  - 本轮修改文件为 `js/ai.js`、`js/constants.js`、`js/main.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - AI 这种系统对“手感时代差”非常敏感，代码能运行不代表逻辑还适合现在的主流程。把远古策略硬接回现代流程，很容易出现“比不改还差”的情况。
  - 清理这类错误接回的旧逻辑时，不能只删入口。和它绑定的状态字段、初始化重置以及常量如果继续残留，会给后续维护造成严重误导，所以这次一并删掉了。
- 沟通坑点：
  - 我上一轮把“想要更强的结果”误理解成“可以直接接回更旧的实现”，这是错误的。用户这次明确指出“这是远古时期的AI代码”“比不改还差”，说明正确方向应是基于当前主逻辑增强，而不是回收历史残片。
  - 后续如果用户再说“想恢复以前那种感觉”，需要先区分他说的是“结果手感”还是“历史实现”，不能默认旧代码就等于旧手感。
- 后续注意事项：
  - 后续若继续增强困难 AI，应优先在当前规划式闪避上做更现代的强化，不要再把仓库里残留的旧闪避策略接回主流程。
  - 用户验证时应重点确认：困难 AI 是否已经回到删除这轮旧代码之前的状态，并且没有出现旧式闪避带来的怪异走位。

### [2026-04-18 15:10] 补充分支测试与合并规则
- 本轮开发内容：
  - 在 `AGENTS.md` 的 `Git 工作流` 中新增了两条强制规则：每轮任务完成后必须明确让用户测试；只有用户测试确认通过后，AI 才能执行任何合并操作。
  - 明确补充了“即使当前已经在支线上，只要开始新的功能开发或 Debug，也必须再新建一个新分支”的要求，避免在旧支线上不断叠加不同任务。
  - 同步更新了 `推荐的实际流程`，把“先新建本轮专用分支、再让用户测试、用户确认后再合并”写成更清晰的步骤。
  - 本轮修改文件为 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这次是规则文档修改，但依然要保持流程一致：先建分支、改完自查、补开发记录、再请用户测试确认。后续即使只是改 `AGENTS.md`，也不能因为“只是文档”就跳过这套流程。
- 沟通坑点：
  - 用户这次说的是“在 git 分支的管理规则中加两条规则”，但真实目标不只是增加两句文字，而是把“完成后必须明确让用户测试”和“支线上做新任务也要继续开新分支”都写成可直接执行的流程约束。
  - 这里还存在一个容易混淆的点：用户说“如果我觉得可以你就合并代码”，重点是“先测试、后合并”，而不是泛泛地“等用户回复再说”。后续遇到类似描述时，要优先把触发条件写成明确动作，而不是只写成宽泛原则。
- 后续注意事项：
  - 后续每轮任务结束后，都必须主动明确告诉用户“现在请测试”，不能只写“你看下有没有问题”这种模糊表达。
  - 后续即使当前已经在某个支线上，只要任务边界变成新的功能或新的 Debug，就必须再切出一个新分支，避免不同任务长期混在同一条支线里。

### [2026-04-18 15:10] 将开发记录时间要求改为精确到分钟
- 本轮开发内容：
  - 在 `AGENTS.md` 的 `记录要求` 中新增了“开发记录标题时间必须精确到分钟，不能只写日期”的强制规则。
  - 同步把 `推荐记录模板` 中的标题占位从“日期”改成了“日期 时间（精确到分钟）”，避免模板和规则不一致。
  - 本轮修改文件为 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这次虽然只是文档规则调整，但如果只改要求、不改模板，后续执行时依然容易继续按旧格式填写，所以规则和模板必须一起更新。
- 沟通坑点：
  - 用户这次说的是“更改一下文件中对记录的要求，要求精确到分钟不要只写一个日期”，重点是“要求”本身要变严格，而不是单纯补一条例子。
  - 这类需求容易误解成“只把新增记录写成带时间即可”，但用户真实意思是让后续整套记录机制都按“精确到分钟”的标准执行。
- 后续注意事项：
  - 后续新增开发记录时，标题必须统一写成 `YYYY-MM-DD HH:mm` 这种精确到分钟的格式。
  - 以后若再调整记录规范，优先同时检查“规则正文”和“模板示例”是否一致，避免一处改了另一处漏改。

### [2026-04-18 15:18] 补充多层分支逐级合并规则
- 本轮开发内容：
  - 在 `AGENTS.md` 的 `Git 工作流` 中新增了多层分支合并规则：如果当前是在“支线的支线”上工作，用户确认后只能先合并回直接上级支线，不能直接一路合并进 `main`。
  - 明确补充了“子分支合并回上级支线后，AI 必须先总结上级支线当前全部修改，再单独询问该上级支线是否继续合并到 `main`”的要求。
  - 同步更新了 `推荐的实际流程`，把“先判断当前分支层级、子分支先回上级、上级是否进 `main` 需再次确认”写成清晰步骤。
  - 本轮修改文件为 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这次虽然只是文档规则补充，但如果只在原则里写“不要直接并进 main”，不把流程步骤也改掉，后续执行时依然容易默认一路合并，所以规则和流程必须一起更新。
- 沟通坑点：
  - 用户这次强调的不是“不要合并”，而是“不同层级的确认只对应不同层级的合并动作”。如果把“用户说 ok”理解成对整条分支链的总授权，就会再次出现错误合并。
  - 这里还有一个容易误解的点：用户要求在子分支回到上级支线后，还要“总结现在全部的修改”，这不是只总结刚刚那一个子分支，而是要把上级支线当前累计的所有修改一起重新说明清楚。
- 后续注意事项：
  - 后续在多层分支结构下，每次准备合并前都要先判断当前分支的直接上级是谁，不能默认最终目标一定是 `main`。
  - 用户对某一层分支说“可以”后，后续回复里要明确告诉用户“这一层我会只合并到哪一层”，避免再次把层级确认说模糊。

### [2026-04-18 15:34] 新增单人模式操控方式设置
- 本轮开发内容：
  - 在 `js/menu.js` 中新增了单人模式操控方式设置，提供 `方向键 + M`、`WASD + 空格`、`鼠标` 三种固定方案，并用 `localStorage` 持久化保存当前选择。
  - 同时把设置页的键盘导航逻辑改成基于设置项列表驱动，不再继续依赖“第几行是什么功能”的硬编码判断，这样在桌面端插入新设置项后，不会把返回键和触摸控制器设置的逻辑顶乱。
  - 在 `js/main.js` 和 `js/tank.js` 中补充了人类坦克的输入分配字段，让单人模式的玩家坦克可以按设置切换到 P1 键位、P2 键位或现有鼠标控制；多人模式仍保持原来的 P1/P2/P3 分配不变。
  - 在 `js/i18n.js` 中补充了新设置项、三个操控方案名称，以及单人模式下主菜单底部提示文案，避免界面显示和真实控制方式不一致。
  - 本轮修改文件为 `js/menu.js`、`js/main.js`、`js/tank.js`、`js/i18n.js` 和 `AGENTS.md`。
- 当前进度：已完成（用户已确认功能没有问题）
- 编码坑点：
  - 这次最容易踩的坑不是“把一个设置存下来”本身，而是设置页原本很多行为都默认绑定在固定行号上。只要直接硬插一行，新选项、返回键、触摸端控制器设置入口就很容易出现选中错位或确认逻辑跑偏。本轮因此先把设置页改成按设置项数组驱动，再插入新选项。
  - 另一个容易误伤的点是当前真人玩家的控制方式原本是按玩家索引硬编码分配的：P1 固定第一套键位，P2 固定第二套键位，P3 固定鼠标。如果只在读取输入时临时改 `KEY_MAPS[i]`，很容易把双人或三人模式一起改坏。本轮因此给坦克单独增加了 `keyboardMapIndex`，把“单人模式的输入选择”和“多人模式的默认分配”拆开处理。
- 沟通坑点：
  - 用户这次描述里已经明确写了“单人模式下（玩家vs 1ai 和玩家 vs 2ai）”，这说明新选项的作用范围只应覆盖 `1pvai` 和 `1pv2ai`。如果只抓住“单人模式”四个字而不看括号补充，后续很容易误改到 `2p`、`2pvai` 或 `3p` 的原有控制方式。
  - 用户要求“提供 2 种键位和 1 种鼠标操控的方式”，但没有额外指定鼠标方案细节，例如是否要键鼠混合、右键开火或新增准星规则。这里如果自己发散设计，很容易做大。本轮采用的正确理解是：优先复用仓库里已经存在的第 3 玩家鼠标方案，只把它开放给单人模式选择。
- 后续注意事项：
  - 后续如果还要继续扩展控制方式，优先复用“设置项列表驱动 + 坦克独立输入映射”的结构，不要再回到按设置页行号和玩家索引硬编码判断的做法。
  - 用户验证时应重点确认两件事：一是桌面端设置页能否正确切换并记住单人操控方式；二是 `1人 vs AI` 和 `1人 vs 2AI` 下的三种方案是否都正常，而 `2人对战`、`2人 + AI`、`3人对战` 的旧控制方式没有被误伤。

## 核心架构特点

### 1. 脚本加载顺序严格依赖（index.html）
**必须按以下顺序加载**，否则会出现未定义错误：
```
constants.js → theme.js → i18n.js → utils.js → state.js → controlsSettings.js 
→ input.js → maze.js → tank.js → bullet.js → particles.js → renderer.js 
→ physics.js → menu.js → controlsConfig.js → ai.js → main.js
```

### 2. 全局变量系统
- 所有常量在 `constants.js` 中定义为全局变量（无 `const`/`let`/`var` 前缀）
- 视口变换变量 `VIEWPORT_SCALE`、`VIEWPORT_OFFSET_X/Y` 使用 `var` 声明（需运行时修改）
- 游戏状态在 `main.js` 中声明为全局 `var`（`maze`、`tanks`、`bullets` 等）

### 3. 物理引擎的独特设计
**关键：输入速度与碰撞速度分离**
- 坦克的 `vx/vy/omega` 包含输入分量和碰撞残余分量
- 使用 `_lastInputVx/Vy/_lastInputOmega` 记录上一帧输入，用于提取碰撞残余
- 碰撞残余以 0.3 系数衰减保留，让坦克旋转时自然滑离墙壁
- **修改物理代码时必须保持这个分离机制**，否则会导致坦克卡墙或无法旋转

### 4. 炮管防穿墙的预测式系统
- `wouldBarrelCollide()` 纯检测函数，不修改任何状态
- `findMaxSafeOmega()` 使用二分搜索限制角速度
- 在 `main.js` 的 `updateGame()` 中，旋转前先预测，碰撞后限制 omega
- **不要在物理碰撞响应中处理炮管旋转限制**，会导致逻辑混乱

### 5. 碰撞检测的多层采样
- `getAllCollisionCorners()` 返回车体角 + 炮管角 + 边缘中点 + 凹槽拐角
- 额外采样点填充车体前脸与炮管之间的凹槽，防止墙体穿入
- **添加新碰撞形状时必须更新采样点**

### 6. 硬约束安全网（`hardConstraintWalls`）
- 在物理迭代后执行，确保绝不穿墙
- 迭代 3 次处理多面墙交汇处
- 推出位置的同时消除穿入方向的速度分量
- **这是最后防线，不要依赖它来替代正常碰撞响应**

### 7. 触摸控制的设备检测逻辑
- 优先使用 User Agent 检测（最可靠）
- 桌面系统即使有触摸 API 也判定为非触摸设备
- 小屏幕 + 触摸支持 → 触摸设备
- **不要简单用 `'ontouchstart' in window` 判断**，会误判触摸屏笔记本

### 8. 摇杆死区机制
- 死区内（`distance <= deadZone`）：只旋转不移动
- 死区外：旋转 + 移动
- 角度始终更新，距离和强度在死区内归零
- **修改摇杆逻辑时必须保持这个行为**

### 9. 胜利缓冲期系统
- 只剩一人时启动 `VICTORY_GRACE_TIME`（2.5秒）缓冲期
- 记录 `pendingWinnerId`，缓冲期结束后重新检测存活状态
- 如果最后存活者也死了 → 平局
- **不要在检测到只剩一人时立即判定胜利**

### 10. 坐标转换链
屏幕坐标 → Canvas 物理坐标 → 游戏逻辑坐标：
```javascript
canvasX = (clientX - rect.left) * (canvas.width / rect.width)
gameX = (canvasX - VIEWPORT_OFFSET_X) / VIEWPORT_SCALE
```
**所有触摸/鼠标事件必须经过这个转换**

## 代码风格
- 使用 ES6 类（`class Tank`），但不使用模块系统
- 函数式辅助工具（`utils.js` 中的 `vecAdd`、`vecRotate` 等）
- 物理引擎中的内部函数使用 `_` 前缀（如 `_applyWallImpulse`）
- 常量使用 `UPPER_SNAKE_CASE`
- 类方法使用 `camelCase`

## 常见陷阱
1. **不要在 `constants.js` 中使用 `const` 声明常量** - 会导致其他文件无法访问
2. **不要修改坦克速度时忘记更新 `_lastInputVx/Vy`** - 会破坏碰撞残余提取
3. **不要在碰撞响应中直接设置 `tank.angle`** - 应该通过 `omega` 和 `integrateVelocity()` 更新
4. **不要跳过 `fitCanvas()` 中的视口变换计算** - 触摸控制依赖这些全局变量
5. **不要在 AI 代码中假设 `tank.input` 会被保留** - 每帧开始时会被重置

### [2026-04-18 16:34] 新增激光道具与反弹激光玩法
- 本轮开发内容：
  - 在对局主循环中新增了地图随机刷新的激光道具系统，当前版本会定时连刷激光道具，场上最多同时存在 2 个。
  - 在 `js/bullet.js` 中新增了通用反弹路径追踪、路径切片工具和 `LaserShot`，让激光可以按真实路径前进、反弹、显示尾迹，并在 25 次碰壁后结束。
  - 在 `js/main.js`、`js/physics.js`、`js/renderer.js`、`js/particles.js` 中接入了道具落地尘埃、激光预览线、激光飞行表现、激光扫线命中判定和拾取逻辑。
  - 在 `js/tank.js` 中增加了坦克的激光持有状态；在 `js/ai.js` 中扩展了电脑逻辑，让 AI 会抢激光、会把激光当作攻击手段，也会把在途激光当作高优先级威胁处理。
  - 在 `js/constants.js`、`js/i18n.js` 中补充了激光/道具相关常量和最少量文案。
  - 本轮修改文件为 `js/constants.js`、`js/tank.js`、`js/bullet.js`、`js/physics.js`、`js/particles.js`、`js/renderer.js`、`js/i18n.js`、`js/ai.js`、`js/main.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最容易误做成“整条激光路径一发出就立刻检查全路径命中”，但用户要的是“激光段前进时扫到才算死”。如果按瞬时全路径判死，手感会完全不对，也会让 5 次反弹后的隐藏路径变成不公平的秒杀线。本轮因此把激光设计成“预先算完整路径，但每帧只按当前头尾区间做扫线判定”。
  - 激光预览、激光飞行和 AI 判断都依赖同一套反弹路径。如果分别写三套近似算法，后面很容易出现“预览看起来能打到，但实际打不到”或“AI 判断能命中，但玩家看到的指示线不是那条路”的错位。本轮因此先补了通用路径追踪和切片工具，再让渲染、命中和 AI 共用。
  - 道具系统原仓库里是完全没有现成骨架的，不能只补一个图标和碰撞判断。真正需要一起补的是：刷新时机、掉落表现、拾取状态、回合重置、AI 目标优先级和 HUD 提示，否则很容易留下“这局有道具、下一局残留”或“AI 完全不理道具”的半成品状态。
- 沟通坑点：
  - 用户原始表达里说“地图上会随机刷新一些道具（现在只做激光道具）”，这里的重点不是马上做完整多道具框架，而是先把“可扩展的道具刷新流程”搭起来，但第一版只落一个激光道具种类。如果把重点理解成一次性做很多道具，会明显超出这轮边界。
  - 用户说“激光武器发射时，显示为一段激光前进，碰壁反弹……在激光段行进时如果碰到就算死，没碰到不算”，这句话明确限定了命中语义是“运动中的激光段”，不是常见的“瞬间射线武器”。后续如果再调激光手感，必须先守住这个语义，不要因为想省实现量又改回瞬时光线。
  - 用户最后补了一句“同时你需要更新电脑的逻辑了”，真实含义不只是“AI 会开激光”这一件事，而是至少包含三块：看到道具会抢、拿到激光会用、看到激光会躲。后续如果用户再说“更新电脑逻辑”，要优先拆开确认是攻击、资源争夺还是生存规避，而不是只改射击部分。
- 后续注意事项：
  - 用户验证时应重点看 4 类结果：道具是否按节奏刷新并落地、激光预览线和真实弹道是否一致、激光扫过才击杀是否符合预期、AI 是否会抢/会用/会躲。
  - 如果后续继续强化激光玩法，优先沿着现有的 `traceRicochetPath` / `sliceRicochetSegments` 这一套公共工具扩展，不要再写平行版本的路径算法。
  - 如果用户后续觉得 AI 的激光表现不够强，优先先调 AI 的激光选角与抢道具优先级，不要先动真实激光命中规则；否则很容易把“AI 不聪明”误修成“武器判定变味”。

### [2026-04-18 17:05] 修正激光预览反弹与视觉风格
- 本轮开发内容：
  - 将激光真实反弹上限从 25 调整为 10，并把激光预览严格截断在前 5 次反弹，不再继续展示后续路径。
  - 统一收敛了激光预览线和真实激光条的线宽，让两者保持一致的视觉粗细，去掉了之前过重的发光效果。
  - 重画了激光道具图标，使其更接近原本 UI 的扁平、硬边风格，不再像单独塞进来的高亮特效图标。
  - 强化了 AI 对激光的威胁判断和躲避优先级，让电脑更早把激光当成高优先级危险处理。
  - 修改文件为 `js/constants.js`、`js/bullet.js`、`js/main.js`、`js/renderer.js`、`js/ai.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户验证
- 编码坑点：
  - 本轮最容易踩的坑是“只改显示，不改路径截断”。如果路径追踪本身不在第 5 次或第 10 次反弹处停止，预览线和真实激光都会继续带出多余的后续段落，导致用户仍然觉得“反弹次数不对”。
  - 激光条和预览线如果分别使用不同的线宽与发光参数，很容易出现“看上去像两套东西”的问题。后续如果再调视觉，优先先把两者统一，再考虑单独做少量差异。
  - AI 对激光的反应不只是“会不会躲”，还包括“会不会第一时间把它当成头号危险”。如果只是扩大预测窗口但不提升优先级，实际表现还是会偏慢。
- 沟通坑点：
  - 用户说“最多 5 次反弹的预览线没有正确实现”，这里的重点其实是“预览要和真实路径共用一套逻辑，但只截取前 5 次反弹”。如果误解成单纯“让线更短”，会导致看起来改了，实际问题仍在。
  - 用户说“激光条也是同样的问题（而且激光条还有过宽的问题，就像预览线一样粗细就行了）”，说明他关注的是整体视觉一致性，不是单个参数；后续遇到类似反馈，应同时看预览、实体和道具三处表现。
  - 用户说“电脑对激光的预判和躲闪能力弱”，这里的“弱”不仅是命中判断慢，也包括回避动作不够坚决。本轮因此把激光威胁直接提到更高优先级，而不是只扩一点预测范围。
- 后续注意事项：
  - 下一轮用户验证时，重点观察：预览是否只到第 5 次反弹、真实激光是否只到第 10 次反弹、激光条是否与预览线粗细一致、AI 是否会更早避开激光。
  - 如果用户仍然觉得 AI 躲避不足，优先继续提升激光威胁的权重和预测跨度，不要先动普通子弹的闪避参数。
### [2026-04-18 17:20] 统一实际激光与预览线样式
- 工作分支：`codex/laser-powerup`
- 本轮开发内容：
  - 在 `js/renderer.js` 中把实际激光的绘制逻辑改成和预览线共用同一套内部绘制函数。
  - 去掉了实际激光之前更重的光束头部和尾焰表现，让它的线宽、圆角、描边和轻微发光都尽量贴近预览线。
  - 这次只改了视觉渲染，没有动激光的命中、反弹、AI 或拾取逻辑。
  - 本轮修改文件为 `js/renderer.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这次比较容易踩的坑是预览线和实弹分别保留两套绘制实现，后续稍微改一处就会又分叉；所以本轮直接抽成同一个内部方法，减少样式漂移。
- 沟通坑点：
  - 用户这次明确说“激光你就把样式改成预览线的样式就行，其他的我觉得可以了”，所以当前需求边界只剩视觉统一，不需要再碰玩法逻辑。
  - 如果后续再说“样式不对”，应优先理解为“和预览线不一致”，而不是继续改激光长度、伤害或 AI 行为。
- 后续注意事项：
  - 下一轮用户验收时，重点看实际激光是否已经和预览线风格一致。
  - 如果再调视觉，优先只动 `js/renderer.js`，避免把战斗系统一起牵进去。

### [2026-04-18 17:35] 记录开发记录中的工作分支名称
- 工作分支：`codex/agents-record-branch-name`
- 本轮开发内容：
  - 在 `AGENTS.md` 的开发记录维护规则中新增了强制要求：每次开发记录都必须写明本轮对应的工作分支名称。
  - 同步把推荐记录模板补了一行 `工作分支`，避免以后只记时间和标题却漏掉分支来源。
  - 本轮修改文件为 `AGENTS.md`。
- 当前进度：已完成
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这类文档规则最容易漏改的地方就是“正文改了，但模板没改”，后续如果再扩展记录要求，应同时检查强制规则和模板示例是否一致。
- 沟通坑点：
  - 用户这次的意思很明确，是要把“分支名”纳入长期记录规范，而不是只在某一次记录里临时补一句。
  - 以后如果用户再说“把某个信息加到记录里”，应优先理解成“加进制度和模板”，而不是只补当前一条记录。
- 后续注意事项：
  - 后续新增开发记录时，除了时间、标题、进度、坑点外，还必须补上当前工作分支名。
  - 如果以后再调整记录模板，先检查是否会影响后续追加记录的稳定格式。
### [2026-04-18 17:41] 加快激光条速度
- 工作分支：`codex/faster-laser-bar`
- 本轮开发内容：
  - 在 `js/constants.js` 中把 `LASER_SPEED` 从 `520` 提高到 `650`，让激光条飞行更快。
  - 本轮只改了激光速度常量，没有改命中、反弹、渲染或 AI 预测逻辑，改动范围很小。
  - 本轮修改文件为 `js/constants.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户验证
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这次虽然只是调一个数值，但它会同时影响实战飞行速度、预览路径和 AI 对激光的判断，所以只动常量、不碰其它逻辑是最稳妥的做法。
- 沟通坑点：
  - 用户说的“激光条”实际对应的是游戏里的激光道具/激光束飞行速度，不是普通子弹，也不是界面进度条。
  - 后续如果再调这类词，最好先按游戏内实际表现来确认对象，避免把道具速度、UI 条和普通武器混在一起。
- 后续注意事项：
  - 用户验证时重点看激光发射后是否明显更快穿过地图，且反弹与命中仍然正常。
  - 如果后续觉得还不够快，可以继续只调 `LASER_SPEED`，先不要连带改其他激光参数。

### [2026-04-18 17:52] 再次加快激光条速度
- 工作分支：`codex/faster-laser-bar-more`
- 本轮开发内容：
  - 在 `js/constants.js` 中把 `LASER_SPEED` 从 `650` 提高到 `780`，继续加快激光条飞行速度。
  - 本轮仍然只改速度常量，没有动命中、反弹、渲染或 AI 预测逻辑。
  - 本轮修改文件为 `js/constants.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户验证
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 连续加速时最需要注意的是别一次拉太猛，否则会把激光的视觉节奏推得太快，反而不好判断命中和反弹。
- 沟通坑点：
  - 用户说“再快一些”，实际意思是继续沿着上一轮的激光速度微调，而不是改成别的武器或别的效果。
  - 后续如果用户再用这种短句反馈，最好默认接着上一轮的具体改动方向理解，而不是重新猜全新的需求。
- 后续注意事项：
  - 用户验证时重点看激光是否比上一版再明显提速，但还不会快到难以观察。
  - 如果用户还想继续加速，仍然只改 `LASER_SPEED` 就够了，先别动别的激光参数。

### [2026-04-18 18:00] 新增任务结束状态更新规则
- 工作分支：`codex/record-task-status-update`
- 本轮开发内容：
  - 在 `AGENTS.md` 的“代码完成后的要求”里新增了一条强制规则：一轮任务真正结束后，AI 必须更新本文件中的本轮任务状态，并明确写成“任务已完成，用户已确认”。
  - 本轮修改文件为 `AGENTS.md`。
- 当前进度：任务已完成，用户已确认
- 编码坑点：
  - 本轮未发现明显编码坑点。
  - 这类规则最容易写得太抽象，所以这次直接把状态文本写死，避免后续出现“已完成”“完成了”“用户确认过了”之类的多种写法，导致记录不统一。
- 沟通坑点：
  - 用户这次说的是“更新文件中的任务状态”，重点不是再新增一条普通说明，而是要把状态更新动作本身变成固定流程。
  - 后续如果用户再提“状态”“完成状态”“任务结束标记”之类的说法，应优先理解成要补到 `AGENTS.md` 的持续维护规则里，而不是只改某条记录的当前进度。
- 后续注意事项：
  - 以后每轮任务真正结束后，除了补开发记录，还要记得同步把状态写成“任务已完成，用户已确认”。
  - 如果后续再调整记录模板，要检查状态文本是否仍然和这条强制规则一致。

### [2026-04-25 10:43] 修复任务完成通知 skill 缺失问题
- 工作分支：`codex/fix-task-complete-notify-skill`
- 本轮开发内容：
  - 补齐了仓库中缺失的 `.agents/skills/task-complete-notify/` 目录，新增 `SKILL.md` 和 `task-complete-notify.ps1`，让完成通知 skill 不再只存在于工具声明里而没有实际文件。
  - 在 `SKILL.md` 中明确了使用场景和调用方式，约定在真正完成实现、修复或编辑任务后，先运行 PowerShell 脚本，再发送最终回复。
  - 在 `task-complete-notify.ps1` 中实现了 3 声提示音和 Windows 弹窗，弹窗内容支持通过 `-Message` 参数传入本轮完成事项。
  - 本轮修改文件为 `.agents/skills/task-complete-notify/SKILL.md`、`.agents/skills/task-complete-notify/task-complete-notify.ps1` 和 `AGENTS.md`。
- 当前进度：任务已完成，用户已确认
- 编码坑点：
  - 这次问题不是脚本内容本身报错，而是 skill 对应的目录和 `SKILL.md` 根本不在仓库里，导致工具在加载阶段就直接找不到路径。后续如果某个 skill 调用失败，不能只盯着脚本语法，还要先确认技能目录、入口文件名和实际路径是否真的存在。
  - 通知脚本运行在 Windows PowerShell 下时，直接用 `MessageBox` 前需要先加载 `System.Windows.Forms`，否则弹窗类型不可用。后续如果继续扩展通知方式，优先保持依赖简单，避免引入额外模块。
- 沟通坑点：
  - 用户这次明确说明“他就是一个 powershell 脚本”，真实需求边界很清楚：重点是把现有 skill 修成“可弹窗、可响铃、可带完成内容”的最小可用版本，而不是设计一套复杂通知系统。
  - 如果后续用户再说“修一下某个 skill”，不能默认问题一定出在脚本逻辑里；也可能像这次一样，是仓库里缺少对应目录或入口文件，属于资源缺失而不是脚本实现错误。
- 后续注意事项：
  - 后续真正完成实现任务并准备发送最终回复前，应优先调用这个 skill 或直接运行其 PowerShell 脚本，确认提示音和弹窗都能出现。
  - 如果以后想改通知样式，优先继续沿用 `-Message` 传参方式，避免把完成内容写死在脚本里。

### [2026-04-25 14:08] 增加激光自杀问题调试程序
- 工作分支：`codex/laser-self-hit-debug`
- 本轮开发内容：
  - 在 `js/main.js` 中加入了激光自杀问题的只读调试状态和记录逻辑，按 `F8` 可以切换调试开关，便于在不改玩法的前提下抓取“发射瞬间”和“命中自己”的现场数据。
  - 在 `js/physics.js` 中让激光命中检测返回更完整的诊断信息，包括命中发生在哪一段扫线、命中点是否从段起点就已经在坦克内部，方便区分“生成就贴身”还是“扫线过程中才撞上自己”。
  - 在 `js/renderer.js` 中新增了调试覆盖层和调试面板，会把发射者轮廓、炮口、激光路径、命中点和文字日志一起画出来，方便直接从画面判断问题原因。
  - 顺手删除了 `js/input.js` 里以前用于打印按键按下的 `console.log('[INPUT] keydown:', e.code);`，避免它和新的激光调试日志混在一起，干扰排查。
  - 本轮修改文件为 `js/main.js`、`js/physics.js`、`js/renderer.js`、`js/input.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 调试程序一开始最容易犯的错，是只在控制台里打印一堆原始对象，但没有把“发射瞬间”和“命中瞬间”拆成可视化信息。本轮改成同时输出文字日志和画面标注，目的是让问题能被肉眼直接对上，而不是只看一坨对象展开。
  - 这类调试代码如果不顺手清掉旧日志，很容易出现“键盘按键日志”和“激光调试日志”混在一起，导致你看不清真正的触发顺序。本轮特地移除了按键 `keydown` 的调试输出，避免噪音污染排查结果。
  - `laserHitTank()` 现在返回的是更完整的命中信息对象，不再只是一个简单命中点。后续如果继续沿用这套调试结构，调用方要注意先判断返回值是否为空，再按对象字段读取，不能再按旧的布尔/点位返回值理解。
- 沟通坑点：
  - 用户这次说“写一个调试程序用于检测这个问题发生得到原因”，真实目标不是直接修掉玩法，而是先做可观测性，确认“为什么前进时会自杀”。如果直接上修复，很可能又把真正原因猜错。
  - 用户还特地提醒“顺便把以前显示按下按键的调试代码删除”，说明他关心的是日志可读性，不只是功能本身。后续遇到多种调试输出同时存在时，要优先替用户清理掉会干扰判断的旧日志。
- 后续注意事项：
  - 下一步请先让用户在浏览器里试 `F8` 打开调试，再复现“前进发射”和“后退发射”两种情况，看面板里记录的是 `segment-start-inside-owner` 还是 `first-sweep-segment-overlap`。
  - 如果日志证实命中点总是先落到发射者自己身上，再进一步做 owner 免伤或安全距离修复；如果不是，则再从炮口位置、尾迹长度或扫线起点继续查。

### [2026-04-25 14:37] 将激光自伤保护改为段号级跳过
- 工作分支：`codex/laser-self-hit-debug`
- 本轮开发内容：
  - 按调试日志确认的问题根因，回退了之前基于 `ownerSafeDistance` 的激光自伤保护，改为在 `js/main.js` 和 `js/physics.js` 里统一按“发射者自身的第 0 段扫线”直接跳过自伤判定。
  - 在 `js/physics.js` 的 `laserHitTank()` 中增加了 owner 保护：如果当前检测对象就是发射者，并且这束激光还处于 `bounceCount === 0` 的初始段阶段，就直接返回不命中。
  - 在 `js/main.js` 的激光命中循环中同步加了同样的段号保护，避免主循环和物理函数对 owner 自伤的判定出现分歧。
  - 退掉了 `js/bullet.js` 里不稳定的 `ownerSafeDistance` 字段，减少后续维护时的误导信息。
  - 本轮修改文件为 `js/bullet.js`、`js/physics.js`、`js/main.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最容易误判的地方，是把“飞行距离不够”当成问题本体。实际上日志已经证明真正触发的是 `segmentIndex === 0` 的初始扫线，所以按距离挡不住问题，必须按段号挡。
  - 自伤保护如果在两个位置重复写，但条件不完全一致，很容易出现“主循环已经跳过了，物理函数又命中了”或相反的分歧。本轮因此统一把 owner 的初始段免伤逻辑写成同一条判断语义：`owner + bounceCount === 0`。
- 沟通坑点：
  - 用户前面说“前进时会立刻自杀，每次都是如此”，从语义上很容易让人继续往“移动速度/安全距离”方向猜。但实际日志显示每次都是初始段命中，不是偶发误差，所以后续遇到类似稳定复现问题时，优先以日志中的 `reason` 和 `segmentIndex` 作为判断依据，不要继续按直觉扩大距离阈值。
- 后续注意事项：
  - 用户验证时重点确认：前进发射是否不再立刻自杀、后退发射是否仍正常、激光反弹后是否仍可命中目标。
  - 如果后续还出现自伤，应优先检查是否还有别的判定入口绕过了 `bounceCount === 0` 的 owner 保护，而不是继续加大距离阈值。

### [2026-04-25 15:11] 删除激光调试代码
- 工作分支：`codex/laser-self-hit-debug`
- 本轮开发内容：
  - 按用户确认“ok大功告成”后，删除了之前为排查自杀问题临时加入的激光调试代码，恢复正常游戏运行时不显示调试面板、不输出 `LASER-DEBUG` 日志的状态。
  - 清理了 `js/main.js` 中的激光调试状态、F8 开关、发射/命中日志采集函数，以及绘制调试覆盖层和面板的调用。
  - 清理了 `js/renderer.js` 中的激光调试覆盖层与调试面板绘制函数。
  - 清理了 `js/physics.js` 中为调试返回的额外命中诊断字段，恢复到只返回实际命中结果的简洁形式。
  - 本轮修改文件为 `js/main.js`、`js/renderer.js`、`js/physics.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户验证
- 编码坑点：
  - 调试代码在修复完成后必须成组移除，不能只删画面不删日志，或者只删开关不删调用。之前这套激光调试一共横跨主循环、物理判定和渲染层，如果只删一层，很容易留下无用依赖。
  - 本轮清理时要特别注意调试辅助字段和正式返回值的边界，不能把仍然需要的激光命中逻辑一起删掉，否则会误伤真实功能。
- 沟通坑点：
  - 用户这次的“把调试代码删除了”指的是整套排查工具都要去掉，不是只删屏幕上的调试框。后续如果用户说“调试代码”“日志”“面板”，最好先确认是要保留哪一部分，避免只清掉一半。
- 后续注意事项：
  - 后续如果再遇到类似自杀问题，需要重新加调试时，优先用临时分支和最小化日志，不要直接把上一轮调试工具原封不动再插回来。
  - 当前已经恢复为正式玩法状态，后续验证重点应放在自伤是否彻底消失，而不是调试面板是否显示。

### [2026-04-26 09:14] 重新加入前进转向自杀调试程序
- 工作分支：`codex/laser-self-hit-turn-debug`
- 本轮开发内容：
  - 按用户反馈“前进时不自杀了，但前进并转向时又会自杀”，重新在 `js/main.js`、`js/physics.js`、`js/renderer.js` 和 `js/input.js` 里加入了激光调试程序。
  - 将调试开关改为数字键 `2`，并恢复了和上次一致的画面高亮 + 左上角文本面板，用于捕获“发射瞬间”和“命中自己”的完整现场数据。
  - 在 `js/main.js` 的发射快照里补充了转向相关信息，包括左右转、前进/后退、`omega`、碰撞速度残余和最终角速度，方便判断问题是否只在“边转边发射”时触发。
  - 在 `js/physics.js` 里继续保留激光命中诊断返回值，并额外标记 owner 候选信息，方便区分“仍然是 owner 初段自伤”还是“转向时碰撞盒变化导致的其他命中路径”。
  - 在 `js/input.js` 里补充了 `Digit2` 的按键防默认行为处理，避免调试键在某些浏览器里和页面默认行为冲突。
  - 本轮修改文件为 `js/main.js`、`js/physics.js`、`js/renderer.js`、`js/input.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户验证
- 编码坑点：
  - 这次和上一次不同，问题不再只是“直线前进发射”而是“前进 + 转向 + 发射”的组合场景，所以如果只记录发射瞬间，不把转向中的角速度、输入方向和碰撞残余一起带出来，很容易看不出问题为何只在转向时复现。
  - 调试面板如果只看命中结果，不显示转向态和运动态，就会把“坦克正在转向”这个关键条件藏掉；本轮因此把转向、运动和命中信息拆开一起记录，减少二次猜测。
- 沟通坑点：
  - 用户这次的描述是“前进并转向时发射激光又会自杀”，这里的重点不是“又回到了旧问题”，而是“触发条件比上一轮更窄”。后续如果再反馈类似问题，要优先把“直走”和“转向”视为不同场景，不要混成一个总现象。
  - 用户明确要求“把调试程序加回来，触发键改为数字2”，说明这次不是只要修复，还要恢复可观测性。后续如果再遇到类似反馈，先保留诊断工具，再考虑是否需要进一步收回。
- 后续注意事项：
  - 用户验证时请重点测试三种动作：直线前进发射、前进转向发射、后退发射，观察调试面板里 `motion` 和 `reason` 是否能区分出问题。
  - 如果日志仍然显示 `segment-start-inside-owner`，下一步就重点看转向时炮口是否再次扫回自己；如果不是，再从炮管碰撞预测和坦克残余角速度继续查。

### [2026-04-26 09:25] 将激光自伤保护改为段号级 owner 跳过
- 工作分支：`codex/laser-self-hit-turn-debug`
- 本轮开发内容：
  - 根据最新调试日志确认：前进并转向时，激光仍然会在第 0 段把 owner 算成命中，而且命中时 `bounceCount` 已经可能变成 1，因此之前按 `bounceCount === 0` 做的保护不稳定。
  - 在 `js/physics.js` 的 `laserHitTank()` 中把 owner 自伤保护改成按 `segmentIndex === 0` 判断，只要命中的就是发射者自身的初始段，就直接跳过，不再依赖 `bounceCount`。
  - 在 `js/main.js` 中移除了主循环里按 `bounceCount` 做的 owner 跳过，避免主循环和物理函数使用不同标准。
  - 保留了调试程序，便于用户继续验证“直走 / 转向 / 后退”三种场景，并观察日志中 `segmentIndex`、`motion` 和 `reason` 的变化。
  - 本轮修改文件为 `js/main.js`、`js/physics.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户验证
- 编码坑点：
  - 这次问题的关键不是“激光飞了多远”，而是“命中的始终是第 0 段，但 `bounceCount` 会在本帧里先更新”。如果把 owner 保护建立在 `bounceCount` 上，就会被帧内时序绕开。
  - 主循环、物理判定和调试输出如果各自使用不同的 owner 保护标准，最后很容易出现“调试看起来像 owner 命中，但实际判定已经绕过”或者相反的分歧，所以这次把标准收敛到 `segmentIndex === 0`。
- 沟通坑点：
  - 用户这次提供的日志非常关键，直接显示“前进 + 转向”时 `motion.turnInput = true`、`motion.move = 1`、`segmentIndex = 0`，说明这不是新的大类问题，而是前一版 owner 保护没有覆盖组合操作场景。
  - 后续如果用户继续提供日志，优先看 `reason`、`segmentIndex`、`bounceCount` 和 `motion`，不要被“又自杀了”这种结果描述带偏到别的方向。
- 后续注意事项：
  - 下一轮用户验证时，重点确认前进转向发射是否已经不再自杀，同时观察调试面板里的 `ownerCandidate` 是否不再出现。
  - 如果仍有问题，再继续从炮口位置和转向时的炮管扫掠碰撞方向查，不要先回退到基于距离的临时阈值。

### [2026-04-26 09:34] 删除激光调试代码
- 工作分支：`codex/laser-self-hit-turn-debug`
- 本轮开发内容：
  - 按用户确认“问题完美解决”后，删除了本轮重新加入的激光调试程序，恢复正常游戏运行时不显示调试面板、不输出 `LASER-DEBUG` 日志的状态。
  - 清理了 `js/main.js` 中的调试状态、数字键 `2` 开关、发射/命中/运动日志采集函数，以及调试覆盖层和面板的调用。
  - 清理了 `js/renderer.js` 中的调试覆盖层与调试面板绘制函数。
  - 清理了 `js/physics.js` 中为调试返回的额外命中诊断字段，恢复到只返回实际命中结果的简洁形式。
  - 清理了 `js/input.js` 中与调试键相关的额外按键拦截，恢复普通按键处理逻辑。
  - 本轮修改文件为 `js/main.js`、`js/renderer.js`、`js/physics.js`、`js/input.js` 和 `AGENTS.md`。
- 当前进度：已完成，待用户确认合并。
- 编码坑点：
  - 调试代码如果横跨输入、主循环、物理判定和渲染层，删除时必须成组清理，不能只删面板不删日志，或者只删开关不删数据结构。否则很容易留下无用引用，后续会误导维护。
  - 清理调试程序时要特别注意不要误删真正的修复逻辑。本轮保留了 `segmentIndex === 0` 的 owner 自伤保护，只移除了用于观察问题的附加日志和画面标记。
- 沟通坑点：
  - 用户这次明确说“问题完美解决，可以把调试代码删除了，并合并入main中了”，说明当前任务边界已经从“继续排查”切换到“清理并合并”。后续如果用户再次说“删除调试代码”，要优先理解为整套工具收尾，而不是只隐藏视觉面板。
- 后续注意事项：
  - 本轮已经只剩正式修复逻辑，下一步应直接进入合并流程，不再保留任何调试入口。
  - 合并前建议再做一次 `git diff --stat` 和 `git status` 确认工作区干净，避免把残留调试改动一起带进主分支。

### [2026-04-25 15:46] 手机端控制器设置页改为真实 1:1 展示
- 工作分支：`feature/mobile-controls-1to1`
- 本轮开发内容：
  - 调整了 `js/controlsConfig.js` 里的控制器设置页布局，不再以“缩小后的预览屏幕”作为主要呈现，而是改为按实际画布尺寸展示控制器位置和大小。
  - 新增了与真实显示区域配套的计算逻辑，让控制器、引导区、操作按钮都围绕实际显示区域重新排布，并在页面上直接标出当前显示比例。
  - 更新了 `js/i18n.js` 中的控制器设置说明文案，把用户看到的说明改成“按实际大小和位置调整触摸控制器”，避免继续误导成普通预览模式。
  - 本轮修改文件为 `js/controlsConfig.js`、`js/i18n.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次改动最容易踩的坑是“把预览变大”和“把实际布局改成 1:1”混为一谈。真正需要改的是投影关系和布局锚点，而不是单纯放大一个预览框。
  - 手机端屏幕宽度不一定和原始画布一致，布局如果继续使用固定像素位置，很容易在窄屏上出现按钮越界或控制器重叠，所以这次把布局改成围绕实际显示区域重新计算，而不是继续沿用原来的固定坐标。
- 沟通坑点：
  - 用户一开始说“不要现在的把屏幕等比例缩小显示一个预览屏幕”，容易被理解成“只要把预览框去掉就行”。实际上用户后面补充的“看到是多大，在哪里，就是怎么样的”才是关键，说明他要的是和真实游戏界面一致的空间关系，而不是视觉上更大一点的预览。
  - 选择“3”以后，真实含义更接近“保持实际比例，同时让整个设置页可以像游戏画面一样跟着视口适配”，而不是简单做一个比原来更大的预览框。后续若再调整这页，优先按“真实布局”和“视口适配”两个维度一起看。
- 后续注意事项：
  - 用户验证时重点看两件事：一是手机端控制器设置页是否已经不再像“缩小预览图”，二是拖动/调整时看到的位置是否和实战中的触摸控制位置一致。
  - 如果用户反馈某些按钮在小屏上挤在一起，优先继续整理控制器设置页的布局锚点，不要先退回到旧预览模式。

### [2026-04-25 15:52] 加深控制器设置页模式切换按钮对比度
- 工作分支：`feature/mobile-controls-1to1`
- 本轮开发内容：
  - 调整了 `js/controlsConfig.js` 中“单人模式 / 双人模式”切换按钮的配色，让未选中态更深、边框更明显，降低手机端看不清的问题。
  - 同时略微提高了选中态的填充和描边对比度，保证当前模式能更清楚地区分出来。
  - 本轮修改文件为 `js/controlsConfig.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这类按钮在控制器设置页里是“模式切换入口”，不是普通装饰按钮，所以颜色太浅会直接影响用户判断当前是在单人还是双人布局。后续调主题时，不能只看整体风格，还要保证这些切换按钮在手机端有足够可读性。
  - 选中态和未选中态如果只差一点点透明度，深色背景下尤其容易糊成一片。本轮把未选中底色和边框都加深，避免依赖单独的文字颜色来撑可读性。
- 沟通坑点：
  - 用户这次明确说“是控制器设置中的按钮”，说明问题对象不是游戏主菜单或其他页面的按钮，而是设置页顶部的模式切换按钮。后续如果再反馈“按钮看不清”，需要先确认具体页面，避免改错位置。
- 后续注意事项：
  - 用户验证时重点看控制器设置页顶部的“单人模式 / 双人模式”按钮是否比之前更容易看清。
  - 如果还觉得浅，可以继续微调这两个按钮的底色和边框，但先不要动整页主题或文字颜色。

### [2026-04-25 17:36] 手机端控制器设置页改为整屏自由摆放
- 工作分支：`feature/mobile-controls-fullscreen`
- 本轮开发内容：
  - 在 `js/controlsConfig.js` 中移除了控制器设置页的预览框、实际区域框和对应的投影/映射逻辑，改成直接按整块屏幕坐标摆放控制器。
  - 取消了原先基于预览框的拖动换算，长按拖动时现在直接读取控制器的真实坐标并在屏幕范围内夹紧，避免按钮拖出去后还要依赖预览区域找回。
  - 保留了参数面板、保存、重置、长按拖动和模式切换这些交互，只去掉“框”的表现和说明。
  - 在 `js/i18n.js` 里把控制器设置说明改成“直接在屏幕上调整触摸控制器”，并删除了不再使用的 `previewArea`、`actualArea` 文案。
  - 本轮修改文件为 `js/controlsConfig.js`、`js/i18n.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最容易踩的坑是“只删预览框的绘制”，但保留原来的坐标映射。那样视觉上虽然看不到框了，拖动和命中仍然会被旧的预览坐标系限制，用户会感觉按钮还是被某个隐藏框卡住。本轮把显示和交互一起改成整屏坐标，避免出现“看起来去掉了，实际上没去掉”的假修复。
  - 控制器设置页里的参数面板原本是跟着预览区域居中计算的，如果不一起改，面板会继续参考旧的框位置。本轮把面板和布局都改成直接依赖当前画布尺寸，避免手机端出现面板偏位。
- 沟通坑点：
  - 用户这次说“去掉它，直接按钮可以在屏幕任意位置放置，也不需要实际区域框”，这里的重点其实有两个：一是不要再显示预览/实际区域框，二是拖动摆放要按整屏自由放置来理解。不能只理解成“把框隐藏”，否则会漏掉交互逻辑。
  - 用户补充的“也不需要实际区域框”说明他想要的是一个更直接的编辑界面，而不是换一种名字继续保留同类框体。后续如果再提类似需求，应优先判断是要删视觉容器，还是连交互坐标系一起删。
- 后续注意事项：
  - 用户验证时重点看三件事：页面上是否已经没有预览框和实际区域框、控制器是否能直接拖到屏幕任意位置、长按拖动后是否仍能正常保存和恢复。
  - 如果用户后续还想继续放宽范围，再决定是否允许越出屏幕边缘；当前版本先按“屏幕内可见范围”处理，避免把控制器拖到完全找不到的位置。

### [2026-04-25 18:13] 控制器配置改为严格 1:1
- 工作分支：`feature/mobile-controls-strict-1to1`
- 本轮开发内容：
  - 在 `js/main.js` 中为控制器设置页引入了临时画布锁定，进入设置页时保持当前画布尺寸不再跟随窗口 resize 变化，退出设置页后再恢复正常的窗口自适应。
  - 在 `js/controlsSettings.js` 中把控制器配置从“比例存储”改为“绝对像素存储”，保存和加载时直接写入 `x / y`，避免设置页和实战因窗口尺寸变化产生偏移。
  - 在 `js/controlsConfig.js` 中继续保留整屏自由摆放，但让编辑坐标和游戏实际坐标使用同一套锁定后的画布尺寸，减少来回切页时的差异。
  - 同步保留了 `js/i18n.js` 中上一轮的“直接在屏幕上调整触摸控制器”说明，本轮不再新增额外文案。
  - 本轮修改文件为 `js/main.js`、`js/controlsSettings.js`、`js/controlsConfig.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次问题的关键不是“显示方式不够直观”，而是“设置页和实际游戏各自使用了不同的画布参考尺寸”。如果只改显示不改存储，拖动时看起来对齐，保存后还是会被比例换算带偏。
  - 绝对像素存储后，最容易被忽视的是窗口 resize。若仍允许设置页在编辑时跟随窗口变化，用户拖动的位置会被当前窗口尺寸重算，结果还是不是真正的 1:1，所以本轮同时锁住了设置页的画布尺寸。
- 沟通坑点：
  - 用户这次明确说“可以，我要严格的1：1”，这里的真实目标不是“看起来差不多”，而是“设置时看到的位置和实际生效位置完全一致”。后续如果再提类似需求，应优先理解为“存储单位和显示单位要统一”，不是只调页面布局。
  - 之前我先按“直接在屏幕上摆放”处理，结果只解决了视觉框问题，没有彻底消除比例换算造成的偏差。这说明用户的“1:1”要求比“去掉预览框”更严格，后续遇到类似表述要先确认是否需要锁定尺寸和坐标单位。
- 后续注意事项：
  - 用户验证时重点看两件事：一是进入控制器设置页后，切回游戏再回来，按钮位置是否完全一致；二是旋转设备或改变窗口大小时，设置页是否仍保持当前编辑尺寸不乱跳。
  - 如果用户后续仍发现偏差，优先检查 `js/main.js` 里进入设置页时是否真的锁住了同一份画布尺寸，再检查 `ControlsSettings` 是否有旧的比例数据残留需要迁移。

### [2026-04-29 19:38] 触摸屏电脑新增界面模式切换
- 工作分支：`feature/touch-desktop-ui-mode-switch`
- 本轮开发内容：
  - 在 `js/input.js` 中把设备识别拆成 `mobile`、`touch-desktop`、`desktop` 三类，新增触摸屏电脑的界面模式持久化键 `tankgame_ui_mode`，让带触摸屏的电脑默认走触摸界面，但允许在运行中切换到电脑界面并保存选择。
  - 在 `js/input.js` 中补了触摸事件的动态绑定与解绑逻辑，不再只在初始化时按设备一次性决定；切到电脑界面后会解绑触摸事件并清空摇杆/开火按钮状态，切回触摸界面再重新绑定。
  - 在 `js/menu.js` 中把菜单可选模式、菜单触摸点击监听、设置项列表都改成按当前 `touchEnabled` / UI 模式来判断，而不是继续把“设备是否支持触摸”和“当前是否显示触摸界面”混为一谈。
  - 在 `js/menu.js` 的设置页中新增了双向切换入口：触摸屏电脑处于触摸界面时显示“切换到电脑界面”，切到电脑界面后显示“切换到手机操作界面”；真正手机不会出现这个切换按钮。
  - 在 `js/i18n.js` 中补充了界面切换相关文案。
  - 本轮修改文件为 `js/input.js`、`js/menu.js`、`js/i18n.js` 和 `AGENTS.md`。
- 当前进度：任务已完成，用户已确认
- 编码坑点：
  - 这次最容易踩的坑是继续沿用旧的 `isTouchDevice` 语义。原项目里它既表示“设备像不像手机”，又表示“当前是否启用触摸界面”；如果不先拆开，触摸屏电脑就无法实现“默认触摸、但可切回电脑界面”的需求。本轮处理方式是先保留旧字段供兼容使用，但把它的含义收敛成“当前 UI 是否为触摸模式”。
  - 触摸事件绑定不能只在 `bindCanvas()` 时做一次。因为这次需要在运行中切界面，如果不补动态解绑，用户切到电脑界面后仍会残留 `touchstart` / `touchend` 监听，容易出现菜单被触摸误触或旧摇杆状态滞留。本轮因此额外抽了 `refreshTouchBindings()` 和 `_resetTouchState()`。
  - 菜单设置页原本已经逐渐往“设置项驱动”结构收敛，但触摸设置布局还带着部分隐式行位假设。本轮继续沿着设置项数组驱动布局，避免新增“切换界面”这一项后又把触摸页行位逻辑写死。
- 沟通坑点：
  - 这轮最关键的需求边界不是“带触摸能力的设备都能切换”，而是“真正手机不能切换，只有带触摸屏的电脑才有切换按钮”。如果只抓住“触摸设备默认走手机界面”，很容易误把手机也开放成可切桌面界面，结果会做出不可用状态。
  - 用户一开始提出的是“设备检测中将带有触摸屏的电脑默认提供和手机一样的界面”，后面又补充了“真正的手机是不能有切换界面的”。这说明后续遇到“和某设备一样的界面”这类说法时，不能默认两个设备的全部交互权限都一样，仍要区分默认样式和可切换能力这两个层级。
- 后续注意事项：
  - 用户验证时应重点看四件事：触摸屏电脑首次进入是否默认触摸界面、设置页是否能切到电脑界面、切回后是否还能再切回触摸界面、刷新页面后是否保持上次选择。
  - 还要额外确认真正手机上没有切换入口、普通电脑上也没有切换入口，并观察电脑界面下 3 人模式是否恢复显示、触摸界面下 3 人模式是否继续隐藏。

### [2026-04-28 21:34] 清理未使用代码与调试残留
- 工作分支：`refactor/code-cleanup-unused-removal`
- 本轮开发内容：
  - 在 `js/utils.js` 中删除了未被任何现有逻辑调用的 `raySegmentIntersect()`，减少无效几何工具残留。
  - 在 `js/main.js` 中删除了只累加和重置、但没有任何消费方的 `roundElapsed` 状态，避免继续保留无意义回合计时变量。
  - 在 `js/input.js` 中删除了设备识别阶段的 `console.log` 调试输出，避免正式运行时打印无关日志。
  - 在 `js/constants.js` 中删除了多项未被引用的常量，包括激光渲染旧参数和触摸控制旧参数，减少常量表噪音。
  - 在 `js/controlsSettings.js` 中删除了 `ControlsSettings.save()` 里未使用的宽高局部变量，收紧保存逻辑。
  - 本轮修改文件为 `js/utils.js`、`js/main.js`、`js/input.js`、`js/constants.js`、`js/controlsSettings.js` 和 `AGENTS.md`。
- 当前进度：任务已完成，用户已确认
- 编码坑点：
  - 这个项目是通过 `index.html` 顺序加载全局脚本的，不能因为某个函数“看起来没用”就直接删，必须先确认有没有被后面脚本以全局方式隐式引用。本轮先做使用链核查，再执行删除，避免误删全局依赖。
  - `constants.js` 里的冗余项看上去只是常量，但一旦删错，影响范围会跨输入、渲染、AI 多个文件，所以删除前必须做全局检索，而不是只看定义附近代码。
  - 做“清理”类任务时，最容易把“异常兜底日志”和“调试残留日志”混为一谈。本轮只删除了 `js/input.js` 中明显无业务价值的设备检测日志，保留了 `menu.js` 与 `controlsSettings.js` 里的异常告警日志。
- 沟通坑点：
  - 用户这次说“做一下代码审查，看有哪些冗余、没用的，删除他们”，边界默认偏大，既可能指真正未引用的死代码，也可能指结构重复、风格不统一的代码。如果不先做分类，很容易把“应该重构”误当成“应该删除”。本轮先把候选项分成“可直接删除 / 应保留 / 仅建议精简”，再执行修改，理解才算对齐。
  - 用户后续回复“开始做完整使用链核查”，说明用户真正要的不是主观判断式清理，而是有证据链的精简删除。后续遇到类似“清理代码”请求，应优先理解成“先证明没用，再删除”，不要直接上手大删。
- 后续注意事项：
  - 后续如果继续做代码洁净化，优先先查 `constants.js`、`utils.js`、`main.js` 这类全局汇聚文件，因为这里最容易沉积历史残留。
  - 再做类似清理时，建议继续保持“小批次删除 + 全局检索复核 + 用户实际测试”这套节奏，避免一次性删太多导致定位困难。

### [2026-05-01 09:08] 产出手机端主界面放大方向 Demo
- 工作分支：`feature/mobile-menu-ui-demos`
- 本轮开发内容：
  - 分析了当前手机端主界面“看起来比较小”的主要原因，确认问题重点不在全局 viewport，而在于主菜单信息密度偏高、卡片高度偏紧、字体与留白不足。
  - 新增了 3 个互不影响正式游戏逻辑的独立 HTML 原型：`demo/mobile-menu-demo-a.html`、`demo/mobile-menu-demo-b.html`、`demo/mobile-menu-demo-c.html`，分别对应“大卡片堆叠版”“分组按钮版”“首页 + 快速开始版”。
  - 新增了 `demo/index.html` 作为导航页，方便在手机上直接逐个打开对比，不需要先改正式界面再看效果。
  - 本轮修改文件为 `demo/index.html`、`demo/mobile-menu-demo-a.html`、`demo/mobile-menu-demo-b.html`、`demo/mobile-menu-demo-c.html` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这轮最容易踩的坑，是一上来就直接改 `js/menu.js` 里的正式触摸菜单。那样虽然可能更快看到效果，但风险是把现有菜单逻辑、触摸命中范围和设置页联动一起带乱，用户也无法横向比较多种视觉方向。本轮因此先用独立静态 demo 承接视觉探索，把“看方向”和“正式落地”拆开处理。
  - 做这种界面对比时，如果 3 个 demo 只是颜色和圆角略有差异，用户很难真正判断哪种结构更适合手机。本轮刻意把 3 套方案拉开成“堆叠卡片 / 分组按钮 / 快速开始首页”三种不同信息组织方式，避免变成无效微调。
- 沟通坑点：
  - 用户这次说的是“你有什么办法解决吗？你可以写几个 html界面的demo供我挑选这样方便我查看到大概的效果，而不是你的文字”，重点不是立刻修正式界面，而是先给出能直接看的方案原型。如果误解成“先选一个我认为最好的方案直接实装”，就会跳过用户真正想要的比较过程。
  - 用户提到的问题是“主界面会看起来比较小，内容不容易看清”，这里的“比较小”更偏视觉可读性，不一定等于画布分辨率、viewport 或 CSS 缩放有 bug。后续再处理类似“看起来小”的反馈时，应先区分是布局密度问题，还是实际缩放计算问题。
- 后续注意事项：
  - 下一步应先让用户在手机上打开 `demo/index.html`，逐个比较 3 套方案里哪一套“看起来更大、更清楚、按钮更好点”。
  - 在用户选定方向之前，不要直接改正式的 `js/menu.js`；等用户明确偏好后，再基于所选方案做小步落地，避免正式代码来回推翻。

### [2026-05-01 09:16] 将手机端主界面 Demo 重做为横屏且贴近原 UI 风格
- 工作分支：`feature/mobile-menu-ui-demos`
- 本轮开发内容：
  - 按用户反馈，放弃了上一版偏普通网页首页风格的竖屏 demo，重新分析了正式触摸界面的视觉来源，确认应以 `TouchUI`、`Theme`、`menu.js` 里的深灰底、点阵面板、等宽字体、三色点缀和轻描边光晕为主。
  - 将 `demo/mobile-menu-demo-a.html`、`demo/mobile-menu-demo-b.html`、`demo/mobile-menu-demo-c.html` 全部重写为“手机横屏优先”的原型，但仍保持 3 种不同信息结构：稳妥放大版、主按钮突出版、快速开局版。
  - 同步重写了 `demo/index.html` 的导航说明，避免用户一打开仍看到旧的竖屏和网页风描述。
  - 本轮修改文件为 `demo/index.html`、`demo/mobile-menu-demo-a.html`、`demo/mobile-menu-demo-b.html`、`demo/mobile-menu-demo-c.html` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最容易犯的错，是只把页面从竖排改成横排，但仍然继续沿用普通网页卡片风格。那样虽然方向改成了横屏，和正式游戏的画布 UI 还是会脱节。本轮因此先回到 `js/utils.js` 里的 `TouchUI` 和 `js/menu.js` 现有触摸菜单实现，先抽出视觉语言，再重做 HTML demo。
  - 做独立 demo 时，如果每个方案连配色和字体体系都换掉，用户最后很难判断自己是在选“布局结构”，还是在选“皮肤风格”。这轮因此把 3 个方案的视觉语言统一收敛，只让布局重心发生变化，避免比较对象混乱。
- 沟通坑点：
  - 用户这次明确指出“手机端默认玩这个游戏都是横屏”，说明我上一轮默认按竖屏网页首页去设计，是把“手机端”错误等同于“竖屏浏览网页”。后续只要涉及这个项目的手机 UI，应优先把“横屏游玩”当成默认前提，而不是普通网站的手机浏览习惯。
  - 用户还指出“主界面的UI风格和程序以前的UI风格大相径庭，简直不搭调”，这说明他要的不是抽象的“更现代网页风”，而是“在现有游戏风格内部做放大和重排”。后续再做界面 demo 时，不能只顾着看单页好不好看，必须先确认它是否还像这个项目本身。
- 后续注意事项：
  - 后续如果正式落地手机主菜单，应优先参考这轮横屏 demo 的信息结构，而不是回退到上一版竖屏网页风原型。
  - 继续推进前，先让用户在横屏手机上实际比较 A / B / C 哪个既看得清、又最像原游戏 UI，再决定正式实现方向。

### [2026-05-01 09:34] 将方案 B 正式落地到触摸主菜单
- 工作分支：`feature/mobile-menu-ui-b-implement`
- 本轮开发内容：
  - 在 `js/menu.js` 中正式把触摸主菜单从原来的单列堆叠布局改为接近方案 B 的横屏结构：左侧是 3 个主要参数卡片，右侧是配置摘要 + 两个更大的动作按钮，底部增加一条统一提示条。
  - 保留了原本 `TouchUI` 的面板、药丸按钮、等宽字体和主题配色，只调整主菜单的布局、尺寸和信息重心，没有额外引入新的 UI 风格。
  - 对选项卡和动作卡做了“宽卡片专用排版”处理，让触摸菜单在横屏下显示更大、更松，开始按钮更醒目，但设置页和触摸事件流程保持不变。
  - 本轮修改文件为 `js/menu.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最容易误伤的是触摸点击逻辑。因为主菜单点击命中完全依赖 `layout.options` 里的矩形范围，如果只改绘制不改布局坐标，或坐标结构改乱了，就会出现“看起来在右边，实际点到左边才触发”的问题。本轮因此坚持只在 `js/menu.js` 里同步改绘制和 `layout.options` 本身，不额外拆事件系统。
  - 另一个容易踩的坑是把触摸主菜单和触摸设置页共用的卡片绘制函数改得过于激进，导致设置页也被连带改坏。本轮通过 `card.h >= 76 && card.w >= 320`、`card.h >= 92` 这种宽卡片条件分支，只让主菜单横屏布局走更宽松的排版，尽量不影响设置页当前尺寸。
- 沟通坑点：
  - 用户说“我选方案b,可以开始修改了”，这里真正含义不是“继续完善 demo”，而是“开始把方案 B 落到真实游戏菜单”。如果还继续在 `demo/` 目录里微调，就会偏离用户当前阶段目标。
  - 因为当前正式实现分支是从 `feature/mobile-menu-ui-demos` 再切出来的子分支，所以后续即使用户确认通过，也只能先合并回直接上级分支，不能因为这轮确认就直接并到 `main`。这类多层分支确认边界需要持续提醒自己，不要省略。
- 后续注意事项：
  - 用户测试时应重点看：横屏手机下主菜单是否明显比之前更大、更清楚；右侧“开始游戏”是否更容易定位；前三个参数卡片左右点击切换是否仍然准确。
  - 如果用户后续觉得还不够像方案 B，优先继续微调 `js/menu.js` 里的主菜单布局比例、右侧摘要区内容和动作按钮高度，不要先去改 `TouchUI` 全局组件，避免把其它界面风格一起带偏。

### [2026-05-01 09:44] 返工方案 B 并让设置页风格靠拢
- 工作分支：`feature/mobile-menu-ui-b-implement`
- 本轮开发内容：
  - 按用户反馈，继续在 `js/menu.js` 中返工触摸主菜单，让结构更贴近 `demo/mobile-menu-demo-b.html`：主菜单改成左侧标题 + 参数分组，右侧主操作区的横屏分栏布局，不再保留上一轮那种“只是大致像 B”的折中版。
  - 同时把触摸设置页也从旧式单列布局改成与方案 B 相同语言的横屏分栏布局，让设置页在标题、信息分组、操作卡片和底部提示条上都更接近主菜单风格。
  - 保留了原有 `TouchUI` / `Theme` 的视觉语言和触摸点击流程，只重排 `layout.options` 坐标和对应绘制层级，没有新增新的系统依赖。
  - 本轮修改文件为 `js/menu.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最大的坑是主菜单和设置页共用一套 `_drawTouchOptionCard()` / `_drawTouchActionCard()`。如果按单一尺寸假设去写，主菜单小卡片、宽卡片和设置页卡片会互相牵连。本轮因此把宽卡片排版做成基于尺寸判断的分支，而不是额外再拆一套重复绘制函数。
  - 触摸命中区域不是根据视觉自动算的，而是完全依赖 `layout.options`。只改画法、不改坐标，或坐标和视觉结构不一致，就会出现“看起来像方案 B，点起来却不是那个位置”的假实现。因此这轮返工时主菜单和设置页都同步重写了布局返回结构。
- 沟通坑点：
  - 用户这次明确指出“这效果和方案B的原设计不一致”，说明我上一轮虽然沿着 B 的方向在做，但还停留在“参考 B”的层面，没有真正按 demo 的结构去落地。后续用户如果说“按某个 demo 改”，应优先理解成尽量复刻该 demo 的信息组织方式，而不是只吸收部分灵感。
  - 用户还补充“同时让设置界面的UI风格向方案B靠拢”，这说明用户关注的不是单一页面，而是整套触摸菜单体验的一致性。后续如果继续调主菜单，默认也要顺带检查设置页是否会显得风格割裂。
- 后续注意事项：
  - 用户测试时应重点看两页：主菜单是否已经更接近方案 B 的原设计；设置页是否也能明显看出同一套横屏 UI 语言。
  - 如果用户后续仍觉得不够像 demo，优先继续微调 `js/menu.js` 中主菜单左/右分栏宽度、设置页左右区域比例、动作卡片高度和标题区留白，而不要先去重构事件或主题系统。

### [2026-05-01 10:17] 让触摸菜单改用更宽的横屏 UI 画布
- 工作分支：`feature/mobile-menu-ui-b-implement`
- 本轮开发内容：
  - 根据用户给出的“真实界面 vs 方案 B demo”对比截图，确认问题不再只是 `js/menu.js` 某几个卡片大小，而是菜单状态本身使用的逻辑画布比例偏窄，导致整套触摸主菜单天然比 demo 小一圈。
  - 在 `js/main.js` 中新增了 `useTouchMenuCanvas()` 和 `useMenuCanvasForCurrentInput()`，让 `STATE.MENU` 与 `STATE.SETTINGS` 在触摸模式下改用更宽的 `16 x 8` UI 参考画布，而桌面菜单和正式对局仍保持原来的画布逻辑。
  - 本轮修改文件为 `js/main.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这次最大的坑是一直在 `js/menu.js` 里硬挤布局，以为只要继续放大右侧卡片就能追上 demo。用户对比图说明那样只能局部变形，追不上根本比例差异。后续如果界面整体“就是比原型小一圈”，要优先检查逻辑画布或参考尺寸，而不是先继续堆局部坐标补丁。
  - 菜单、设置页和控制器设置页都属于 UI 状态，但它们不一定应该共用同一套画布尺寸。本轮因此只把 `STATE.MENU` / `STATE.SETTINGS` 切到更宽的触摸菜单画布，没有顺手改 `STATE.CONTROLS_CONFIG`，避免把控制器编辑页一起带偏。
- 沟通坑点：
  - 用户前面连续说“还是不够大”，如果只按字面继续理解成“再把某个框加大一点”，就会一直在错误层级上微调。直到用户给出“真实效果 vs 方案 B demo”的两张对比截图，才明确这是整套 UI 比例层面的差异，不是单个组件尺寸问题。
  - 这说明当用户连续两次反馈“还是不对 / 还是不够大”时，应尽快让问题进入“截图对比 + 结构层级判断”，不要继续只按主观想象做小改。
- 后续注意事项：
  - 用户这轮验证时应重点确认：主菜单和设置页是否终于整体更接近 demo 的横向体量，而不是只看某一张卡是否更大。
  - 如果用户后续还觉得和 demo 有差距，优先先看更宽逻辑画布下的 `js/menu.js` 分栏比例是否还需要细调，不要立刻再改回单纯放大某个局部组件。


### [2026-05-01 14:22] 修复手机端地图大小设置按钮溢出
- 工作分支：`feature/map-size-settings`
- 本轮开发内容：
  - 只在 `js/menu.js` 中调整了触摸设置页和地图大小二级页的卡片布局与绘制逻辑，没有改动地图范围持久化、随机地图生成或桌面设置页的数据接线。
  - 让触摸设置页右侧卡片高度按项目数量自适应收缩，避免地图大小二级页在 4 个调节项加返回项时把卡片整体挤出面板范围。
  - 给触摸调节卡片和动作卡片增加了基于可用宽度的字体自适应与文本裁剪区域，同时缩小紧凑卡片中的左右按钮、OK 按钮和底部数值条，减少“最小宽度 / 最大高度”等较长文案在手机端溢出框体的问题。
- 当前进度：任务已完成，用户已确认
- 编码坑点：
  - 这次问题不只是某个按钮坐标错了一点，而是“地图大小”功能加入后，触摸设置页右侧项目数变多，原先按固定 96 高度排卡片的写法已经不适合所有页面。如果只微调单个按钮位置，不处理列表总高度，最后仍会在手机横屏下挤出面板。
  - 触摸 UI 的命中区域完全依赖 `layout.options`，所以修这种溢出时不能只改绘制尺寸，不改布局分配；否则会出现视觉上缩回去了，但点击还停留在旧位置的错位。本轮保持绘制和命中继续共用同一套卡片矩形，避免这种假修复。
  - 长文件上连续打补丁时，这个仓库的工具环境会出现“部分命中、部分没改到”的情况。本轮就遇到了 `js/menu.js` 第一次只改进了一半的情况，后续在这种大文件上应优先分段读回关键区块，确认最新基线后再补丁，避免留下半接线状态。
- 沟通坑点：
  - 用户这次明确说的是“手机端 UI 中按钮超出图标框范围了”，重点是触摸界面的视觉和布局，不是地图大小功能本身有 bug。如果只抓住“地图大小”四个字，很容易误改到持久化、随机地图范围或桌面设置逻辑。
  - 用户还特别强调“不要顺手重构无关菜单”，这说明本轮边界非常窄，正确理解应是“只修手机端溢出，不趁机整理整个设置页结构”。后续遇到类似反馈，要优先把范围收紧到用户指出的那一层。
- 后续注意事项：
  - 用户验证时应重点看触摸设置页中的“地图大小”入口卡片，以及地图大小二级页里的 4 个参数项和返回项，确认标题、提示文案、数值条、左右按钮都没有超出卡片。
  - 如果用户后续仍觉得某一项偏挤，优先继续微调 `js/menu.js` 中触摸卡片的紧凑参数和字体缩放，不要先去改数据逻辑或桌面布局。

### [2026-05-01 16:49] 为菜单与状态切换补充轻量动画
- 工作分支：`feature/ui-light-animations`
- 本轮开发内容：
  - 在 `js/menu.js` 中给主菜单、设置页、地图大小页补了轻量切页动画，使用页面进入时的淡入 + 轻微横向滑入，只作用在绘制层，不改现有页面状态机和 `layout.options` 命中区域。
  - 在 `js/menu.js` 中给键盘选中项补了平滑高亮过渡，并给触摸卡片补了短促 pulse / 高亮反馈，保持原有点击判定和左右半区切换逻辑不变。
  - 在 `js/main.js` 中增加了全屏淡入淡出过渡驱动，覆盖“菜单开始游戏”和“游戏结束回菜单”两类硬切场景，切换动作发生在遮罩最深时刻，不改游戏流程本身。
  - 在 `js/renderer.js` 中增加了全屏过渡遮罩绘制方法，并让回合提示 / 胜负提示支持淡入 + 轻微位移 + 轻微缩放入场。
  - 本轮修改文件为 `js/menu.js`、`js/main.js`、`js/renderer.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 这轮最容易误伤的点是把动画状态接进交互逻辑。菜单触摸判定完全依赖 `layout.options`，如果为了做 pulse 或滑入去改卡片实际命中矩形，就会出现“看起来在动，点起来错位”的问题。本轮因此坚持只改绘制层的 alpha / offset / scale，不改命中框。
  - `js/menu.js` 是长文件，连续大块补丁时工具环境多次出现“部分命中、部分未改”的情况。如果继续盲补，很容易把动画辅助状态和旧调用方式混在一起。本轮后半段改成分块读取 + 定点替换，才避免留下半接线状态。后续在这种大文件上应优先小步改、及时读回关键区块。
  - 回合提示动画如果只改 `renderer.drawRoundMessage()`，但主循环没有提供“何时重新开始入场”的进度来源，动画表面上支持了参数，实际不会生效。本轮因此补了 `roundMessageAnim` 的文本/状态变更检测，只在提示文本或提示状态变化时重置入场计时，避免影响原本暂停时长。
- 沟通坑点：
  - 本轮未发现明显沟通坑点。
  - 用户对边界描述非常明确：只要轻量动画、低风险、小改动，不要扩展到 `js/controlsConfig.js`，也不要改玩法层。这类需求如果自己顺手“优化体验”过头，最容易越界，所以本轮始终按“只在绘制层加动画”收紧实现范围。
- 后续注意事项：
  - 用户验证时应重点看 4 类结果：菜单/设置/地图大小页切换是否更顺；键盘选中高亮是否平滑；触摸卡片点击是否有轻微反馈但不影响点击位置；开始游戏和游戏结束回菜单是否出现短促全屏淡入淡出。
  - 还要重点确认回合提示和胜负提示的停留时长没有变化，只是显示更柔和；如果用户反馈“提示时机不对”或“点击区域变了”，应优先检查是否误把动画状态接进了逻辑层，而不是继续叠加更多动画。

### [2026-05-01 17:09] 修复 UI 动画回归问题
- 工作分支：`fix/ui-animation-regressions`
- 本轮开发内容：
  - 在 `js/menu.js` 中把桌面端键盘菜单项的高亮绘制抽成 `_drawKeyboardOptionText()`，统一基础文本和高亮文本的字号、坐标与绘制基线，只保留颜色和轻微 glow 的渐变，修复选中高亮与原文本错位叠画的问题。
  - 在 `js/main.js` 中新增并接入 `syncRoundMessageAnimation()` 的统一同步时机：进入 `ROUND_PAUSE` / `GAME_OVER` 的那一刻立即重置提示动画起点，主循环每帧统一校准一次，并在回到 `PLAYING` 或 `MENU` 时清理提示动画状态。
  - 本轮重点检查了 `js/renderer.js` 中的 `drawRoundMessage()`，确认渲染层本身的 alpha / 位移 / 缩放公式可以继续沿用，因此这次没有改它，只修正动画状态初始化与重置时机。
  - 本轮只动了 `js/menu.js`、`js/main.js`、`js/renderer.js` 的这两个问题相关路径，没有扩展到触摸菜单反馈、控制器设置页、AI、物理、碰撞、地图生成或输入坐标换算。
  - 本轮修改文件为 `js/menu.js`、`js/main.js` 和 `AGENTS.md`。
- 当前进度：待用户验证
- 编码坑点：
  - 桌面端菜单高亮如果直接用两次 `fillText()` 叠加，但两次使用不同字号或字重，就算坐标完全一样，也会因为字形包围盒不同而出现视觉错位和重影。后续做“高亮叠画”时，优先保持同一字体参数，只改颜色、透明度或轻微外发光。
  - 回合提示动画的问题根因不在 `renderer.drawRoundMessage()` 本身，而在状态切换的那一帧：如果动画起点只在主循环下一帧才重置，首次进入暂停/胜负态时就会先拿旧进度画出一帧，造成“先闪一下再正常渐显”。后续凡是做状态型 UI 动画，都要优先确认“进入状态的当帧”是否已经完成动画起点初始化。
  - 本轮再次遇到长文件补丁“部分命中、部分没改到”的情况，尤其是在 `js/main.js` 上分段接线时更明显。后续在这类文件上应继续坚持“小块读取当前基线 → 精确替换 → 再读回确认”的方式，避免留下半修复状态。
- 沟通坑点：
  - 用户这次明确说“只修这两个问题，不要扩展别的优化”，真实边界非常严格。如果只看到“动画问题”四个字，很容易顺手继续改更多 easing、时长或视觉参数，结果就越界了。本轮正确做法是保持上一轮动画方向不变，只修高亮错位和提示动画时机两处根因。
  - 用户把第二个问题描述成“时有时无，而且会先闪一下，再出现正常的逐渐出现”，这里同时包含“触发不稳定”和“首帧错误显示”两个层面。后续遇到类似表述时，要注意不要只修其中一半，例如只修闪烁但没修触发时机，或反过来只修重置但忽略首帧表现。
- 后续注意事项：
  - 用户验证时应重点在桌面端用键盘上下切换主菜单和设置页，确认高亮文字始终和底层文本完全重合，没有双影或偏移。
  - 同时应多次观察击杀后比分提示、平局提示、整局胜利提示，确认每次都是从初始低 alpha / 轻微下移 / 轻微缩放开始渐显，不会先亮一帧最终态，也不会有时触发有时不触发。

